import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'PKU Daily Limitter',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
